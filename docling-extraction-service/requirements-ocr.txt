# OCR-capable image lockfile: default stack + Surya/torch + Google Document AI.
# Build with:  docker build --build-arg REQUIREMENTS=requirements-ocr.txt ...
# CPU-only torch wheels are selected in the Dockerfile via the pytorch CPU index.

# --- everything from requirements.txt ---
-r requirements.txt

# --- OCR stack (pinned from tested environment) ---
# +cpu local version tags force the CPU-only torch wheels from
# https://download.pytorch.org/whl/cpu (passed as --extra-index-url by the
# OCR image build) — avoids pulling multi-GB CUDA runtimes.
surya-ocr==0.22.1
torch==2.13.0+cpu
torchvision==0.28.0+cpu
transformers==5.15.1
tokenizers==0.22.2
safetensors==0.8.0
huggingface_hub==1.28.0
hf-xet==1.6.0
sympy==1.14.0
mpmath==1.3.0
networkx==3.6.1
fsspec==2026.7.0
filelock==3.32.3

# Document AI path (preferred when GOOGLE_CLOUD_PROJECT is configured)
google-cloud-documentai>=0.20

# OpenCV is only required by Surya's detection heatmaps; headless variant for containers.
opencv-python-headless>=4.9
