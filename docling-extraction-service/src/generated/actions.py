# -*- coding: utf-8 -*-
# code generated by Prisma. DO NOT EDIT.
# pyright: reportUnusedImport=false
# fmt: off
from __future__ import annotations

# global imports for type checking
from builtins import bool as _bool
from builtins import int as _int
from builtins import float as _float
from builtins import str as _str
import sys
import decimal
import datetime
from typing import (
    TYPE_CHECKING,
    Optional,
    Iterable,
    Iterator,
    Sequence,
    Callable,
    ClassVar,
    NoReturn,
    TypeVar,
    Generic,
    Mapping,
    Tuple,
    Union,
    List,
    Dict,
    Type,
    Any,
    Set,
    overload,
    cast,
)
from typing_extensions import TypedDict, Literal


LiteralString = str
# -- template actions.py.jinja --
from typing import TypeVar
import warnings

from . import types, errors, bases
from ._compat import model_parse
from ._constants import CREATE_MANY_SKIP_DUPLICATES_UNSUPPORTED

if TYPE_CHECKING:
    from .client import Prisma
    from .bases import _PrismaModel


_PrismaModelT = TypeVar('_PrismaModelT', bound='_PrismaModel')


class LessonMaterialActions(Generic[_PrismaModelT]):
    __slots__ = (
        '_client',
        '_model',
    )

    def __init__(self, client: Prisma, model: Type[_PrismaModelT]) -> None:
        self._client = client
        self._model = model

    async def query_raw(
        self,
        query: LiteralString,
        *args: Any,
    ) -> List[_PrismaModelT]:
        """Execute a raw SQL query

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        List[prisma.models.LessonMaterial]
            The records returned by the SQL query

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        users = await LessonMaterial.prisma().query_raw(
            'SELECT * FROM LessonMaterial WHERE id = $1',
            'bbadfchfja',
        )
        ```
        """
        return await self._client.query_raw(query, *args, model=self._model)

    async def query_first(
        self,
        query: LiteralString,
        *args: Any,
    ) -> Optional[_PrismaModelT]:
        """Execute a raw SQL query, returning the first result

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        prisma.models.LessonMaterial
            The first record returned by the SQL query
        None
            The raw SQL query did not return any records

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        user = await LessonMaterial.prisma().query_first(
            'SELECT * FROM LessonMaterial WHERE user_id = $1',
            'dhheabfhf',
        )
        ```
        """
        return await self._client.query_first(query, *args, model=self._model)

    async def create(
        self,
        data: types.LessonMaterialCreateInput,
        include: Optional[types.LessonMaterialInclude] = None
    ) -> _PrismaModelT:
        """Create a new LessonMaterial record.

        Parameters
        ----------
        data
            LessonMaterial record data
        include
            Specifies which relations should be loaded on the returned LessonMaterial model

        Returns
        -------
        prisma.models.LessonMaterial
            The created LessonMaterial record

        Raises
        ------
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # create a LessonMaterial record from just the required fields
        lessonmaterial = await LessonMaterial.prisma().create(
            data={
                # data to create a LessonMaterial record
                'user_id': 'ggciceaie',
                'lesson_id': 'bbehjachib',
                'title': 'cadfabfehe',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='create',
            model=self._model,
            arguments={
                'data': data,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def create_many(
        self,
        data: List[types.LessonMaterialCreateWithoutRelationsInput],
        *,
        skip_duplicates: Optional[bool] = None,
    ) -> int:
        """Create multiple LessonMaterial records at once.

        This function is *not* available when using SQLite.

        Parameters
        ----------
        data
            List of LessonMaterial record data
        skip_duplicates
            Boolean flag for ignoring unique constraint errors

        Returns
        -------
        int
            The total number of records created

        Raises
        ------
        prisma.errors.UnsupportedDatabaseError
            Attempting to query when using SQLite
        prisma.errors.UniqueViolationError
            A unique constraint check has failed, these can be ignored with the `skip_duplicates` argument
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        total = await LessonMaterial.prisma().create_many(
            data=[
                {
                    # data to create a LessonMaterial record
                    'user_id': 'dgiiaaijj',
                    'lesson_id': 'bfaiacjjfc',
                    'title': 'eigcfgbif',
                },
                {
                    # data to create a LessonMaterial record
                    'user_id': 'bagcfbhiig',
                    'lesson_id': 'cghideieh',
                    'title': 'biabhbdai',
                },
            ],
            skip_duplicates=True,
        )
        ```
        """
        if skip_duplicates and self._client._active_provider in CREATE_MANY_SKIP_DUPLICATES_UNSUPPORTED:
            raise errors.UnsupportedDatabaseError(self._client._active_provider, 'create_many_skip_duplicates')

        resp = await self._client._execute(
            method='create_many',
            model=self._model,
            arguments={
                'data': data,
                'skipDuplicates': skip_duplicates,
            },
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    async def delete(
        self,
        where: types.LessonMaterialWhereUniqueInput,
        include: Optional[types.LessonMaterialInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Delete a single LessonMaterial record.

        Parameters
        ----------
        where
            LessonMaterial filter to select the record to be deleted, must be unique
        include
            Specifies which relations should be loaded on the returned LessonMaterial model

        Returns
        -------
        prisma.models.LessonMaterial
            The deleted LessonMaterial record
        None
            Could not find a record to delete

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        lessonmaterial = await LessonMaterial.prisma().delete(
            where={
                'id': 'idghgaicb',
            },
        )
        ```
        """
        try:
            resp = await self._client._execute(
                method='delete',
                model=self._model,
                arguments={
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    async def find_unique(
        self,
        where: types.LessonMaterialWhereUniqueInput,
        include: Optional[types.LessonMaterialInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Find a unique LessonMaterial record.

        Parameters
        ----------
        where
            LessonMaterial filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned LessonMaterial model

        Returns
        -------
        prisma.models.LessonMaterial
            The found LessonMaterial record
        None
            No record matching the given input could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        lessonmaterial = await LessonMaterial.prisma().find_unique(
            where={
                'id': 'fjfddhigg',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_unique',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None
        return model_parse(self._model, result)

    async def find_unique_or_raise(
        self,
        where: types.LessonMaterialWhereUniqueInput,
        include: Optional[types.LessonMaterialInclude] = None
    ) -> _PrismaModelT:
        """Find a unique LessonMaterial record. Raises `RecordNotFoundError` if no record is found.

        Parameters
        ----------
        where
            LessonMaterial filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned LessonMaterial model

        Returns
        -------
        prisma.models.LessonMaterial
            The found LessonMaterial record

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        lessonmaterial = await LessonMaterial.prisma().find_unique_or_raise(
            where={
                'id': 'hjaecfifb',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_unique_or_raise',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def find_many(
        self,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.LessonMaterialWhereInput] = None,
        cursor: Optional[types.LessonMaterialWhereUniqueInput] = None,
        include: Optional[types.LessonMaterialInclude] = None,
        order: Optional[Union[types.LessonMaterialOrderByInput, List[types.LessonMaterialOrderByInput]]] = None,
        distinct: Optional[List[types.LessonMaterialScalarFieldKeys]] = None,
    ) -> List[_PrismaModelT]:
        """Find multiple LessonMaterial records.

        An empty list is returned if no records could be found.

        Parameters
        ----------
        take
            Limit the maximum number of LessonMaterial records returned
        skip
            Ignore the first N results
        where
            LessonMaterial filter to select records
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned LessonMaterial model
        order
            Order the returned LessonMaterial records by any field
        distinct
            Filter LessonMaterial records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        List[prisma.models.LessonMaterial]
            The list of all LessonMaterial records that could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the first 10 LessonMaterial records
        lessonmaterials = await LessonMaterial.prisma().find_many(take=10)

        # find the first 5 LessonMaterial records ordered by the lesson_id field
        lessonmaterials = await LessonMaterial.prisma().find_many(
            take=5,
            order={
                'lesson_id': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_many',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return [model_parse(self._model, r) for r in resp['data']['result']]

    async def find_first(
        self,
        skip: Optional[int] = None,
        where: Optional[types.LessonMaterialWhereInput] = None,
        cursor: Optional[types.LessonMaterialWhereUniqueInput] = None,
        include: Optional[types.LessonMaterialInclude] = None,
        order: Optional[Union[types.LessonMaterialOrderByInput, List[types.LessonMaterialOrderByInput]]] = None,
        distinct: Optional[List[types.LessonMaterialScalarFieldKeys]] = None,
    ) -> Optional[_PrismaModelT]:
        """Find a single LessonMaterial record.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            LessonMaterial filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned LessonMaterial model
        order
            Order the returned LessonMaterial records by any field
        distinct
            Filter LessonMaterial records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.LessonMaterial
            The first LessonMaterial record found, matching the given arguments
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second LessonMaterial record ordered by the title field
        lessonmaterial = await LessonMaterial.prisma().find_first(
            skip=1,
            order={
                'title': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_first',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None

        return model_parse(self._model, result)

    async def find_first_or_raise(
        self,
        skip: Optional[int] = None,
        where: Optional[types.LessonMaterialWhereInput] = None,
        cursor: Optional[types.LessonMaterialWhereUniqueInput] = None,
        include: Optional[types.LessonMaterialInclude] = None,
        order: Optional[Union[types.LessonMaterialOrderByInput, List[types.LessonMaterialOrderByInput]]] = None,
        distinct: Optional[List[types.LessonMaterialScalarFieldKeys]] = None,
    ) -> _PrismaModelT:
        """Find a single LessonMaterial record. Raises `RecordNotFoundError` if no record was found.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            LessonMaterial filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned LessonMaterial model
        order
            Order the returned LessonMaterial records by any field
        distinct
            Filter LessonMaterial records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.LessonMaterial
            The first LessonMaterial record found, matching the given arguments

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second LessonMaterial record ordered by the language field
        lessonmaterial = await LessonMaterial.prisma().find_first_or_raise(
            skip=1,
            order={
                'language': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_first_or_raise',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def update(
        self,
        data: types.LessonMaterialUpdateInput,
        where: types.LessonMaterialWhereUniqueInput,
        include: Optional[types.LessonMaterialInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Update a single LessonMaterial record.

        Parameters
        ----------
        data
            LessonMaterial record data specifying what to update
        where
            LessonMaterial filter to select the unique record to create / update
        include
            Specifies which relations should be loaded on the returned LessonMaterial model

        Returns
        -------
        prisma.models.LessonMaterial
            The updated LessonMaterial record
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        lessonmaterial = await LessonMaterial.prisma().update(
            where={
                'id': 'cbbbjbfcii',
            },
            data={
                # data to update the LessonMaterial record to
            },
        )
        ```
        """
        try:
            resp = await self._client._execute(
                method='update',
                model=self._model,
                arguments={
                    'data': data,
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    async def upsert(
        self,
        where: types.LessonMaterialWhereUniqueInput,
        data: types.LessonMaterialUpsertInput,
        include: Optional[types.LessonMaterialInclude] = None,
    ) -> _PrismaModelT:
        """Updates an existing record or create a new one

        Parameters
        ----------
        where
            LessonMaterial filter to select the unique record to create / update
        data
            Data specifying what fields to set on create and update
        include
            Specifies which relations should be loaded on the returned LessonMaterial model

        Returns
        -------
        prisma.models.LessonMaterial
            The created or updated LessonMaterial record

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        lessonmaterial = await LessonMaterial.prisma().upsert(
            where={
                'id': 'bbejhfidcb',
            },
            data={
                'create': {
                    'id': 'bbejhfidcb',
                    'user_id': 'bagcfbhiig',
                    'lesson_id': 'cghideieh',
                    'title': 'biabhbdai',
                },
                'update': {
                    'user_id': 'bagcfbhiig',
                    'lesson_id': 'cghideieh',
                    'title': 'biabhbdai',
                },
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='upsert',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
                'create': data.get('create'),
                'update': data.get('update'),
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def update_many(
        self,
        data: types.LessonMaterialUpdateManyMutationInput,
        where: types.LessonMaterialWhereInput,
    ) -> int:
        """Update multiple LessonMaterial records

        Parameters
        ----------
        data
            LessonMaterial data to update the selected LessonMaterial records to
        where
            Filter to select the LessonMaterial records to update

        Returns
        -------
        int
            The total number of LessonMaterial records that were updated

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # update all LessonMaterial records
        total = await LessonMaterial.prisma().update_many(
            data={
                'status': 'bgeecijdgg'
            },
            where={}
        )
        ```
        """
        resp = await self._client._execute(
            method='update_many',
            model=self._model,
            arguments={'data': data, 'where': where,},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    @overload
    async def count(
        self,
        select: None = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.LessonMaterialWhereInput] = None,
        cursor: Optional[types.LessonMaterialWhereUniqueInput] = None,
    ) -> int:
        """Count the number of LessonMaterial records present in the database

        Parameters
        ----------
        select
            Select the LessonMaterial fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            LessonMaterial filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.LessonMaterialCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = await LessonMaterial.prisma().count()

        # results: prisma.types.LessonMaterialCountAggregateOutput
        results = await LessonMaterial.prisma().count(
            select={
                '_all': True,
                'created_at': True,
            },
        )
        ```
        """


    @overload
    async def count(
        self,
        select: types.LessonMaterialCountAggregateInput,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.LessonMaterialWhereInput] = None,
        cursor: Optional[types.LessonMaterialWhereUniqueInput] = None,
    ) -> types.LessonMaterialCountAggregateOutput:
        ...

    async def count(
        self,
        select: Optional[types.LessonMaterialCountAggregateInput] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.LessonMaterialWhereInput] = None,
        cursor: Optional[types.LessonMaterialWhereUniqueInput] = None,
    ) -> Union[int, types.LessonMaterialCountAggregateOutput]:
        """Count the number of LessonMaterial records present in the database

        Parameters
        ----------
        select
            Select the LessonMaterial fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            LessonMaterial filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.LessonMaterialCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = await LessonMaterial.prisma().count()

        # results: prisma.types.LessonMaterialCountAggregateOutput
        results = await LessonMaterial.prisma().count(
            select={
                '_all': True,
                'updated_at': True,
            },
        )
        ```
        """

        # TODO: this selection building should be moved to the QueryBuilder
        #
        # note the distinction between checking for `not select` here and `select is None`
        # later is to handle the case that the given select dictionary is empty, this
        # is a limitation of our types.
        if not select:
            root_selection = ['_count { _all }']
        else:

            root_selection = [
                '_count {{ {0} }}'.format(' '.join(k for k, v in select.items() if v is True))
            ]

        resp = await self._client._execute(
            method='count',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'cursor': cursor,
            },
            root_selection=root_selection,
        )

        if select is None:
            return cast(int, resp['data']['result']['_count']['_all'])
        else:
            return cast(types.LessonMaterialCountAggregateOutput, resp['data']['result']['_count'])

    async def delete_many(
        self,
        where: Optional[types.LessonMaterialWhereInput] = None
    ) -> int:
        """Delete multiple LessonMaterial records.

        Parameters
        ----------
        where
            Optional LessonMaterial filter to find the records to be deleted

        Returns
        -------
        int
            The total number of LessonMaterial records that were deleted

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # delete all LessonMaterial records
        total = await LessonMaterial.prisma().delete_many()
        ```
        """
        resp = await self._client._execute(
            method='delete_many',
            model=self._model,
            arguments={'where': where},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    # TODO: make this easier to work with safely, currently output fields are typed as
    #       not required, we should refactor the return type
    # TODO: consider returning a Dict where the keys are a Tuple of the `by` selection
    # TODO: statically type that the order argument is required when take or skip are present
    async def group_by(
        self,
        by: List['types.LessonMaterialScalarFieldKeys'],
        *,
        where: Optional['types.LessonMaterialWhereInput'] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        avg: Optional['types.LessonMaterialAvgAggregateInput'] = None,
        sum: Optional['types.LessonMaterialSumAggregateInput'] = None,
        min: Optional['types.LessonMaterialMinAggregateInput'] = None,
        max: Optional['types.LessonMaterialMaxAggregateInput'] = None,
        having: Optional['types.LessonMaterialScalarWhereWithAggregatesInput'] = None,
        count: Optional[Union[bool, 'types.LessonMaterialCountAggregateInput']] = None,
        order: Optional[Union[Mapping['types.LessonMaterialScalarFieldKeys', 'types.SortOrder'], List[Mapping['types.LessonMaterialScalarFieldKeys', 'types.SortOrder']]]] = None,
    ) -> List['types.LessonMaterialGroupByOutput']:
        """Group LessonMaterial records by one or more field values and perform aggregations
        each group such as finding the average.

        Parameters
        ----------
        by
            List of scalar LessonMaterial fields to group records by
        where
            LessonMaterial filter to select records
        take
            Limit the maximum number of LessonMaterial records returned
        skip
            Ignore the first N records
        avg
            Adds the average of all values of the specified fields to the `_avg` field
            in the returned data.
        sum
            Adds the sum of all values of the specified fields to the `_sum` field
            in the returned data.
        min
            Adds the smallest available value for the specified fields to the `_min` field
            in the returned data.
        max
            Adds the largest available value for the specified fields to the `_max` field
            in the returned data.
        count
            Adds a count of non-fields to the `_count` field in the returned data.
        having
            Allows you to filter groups by an aggregate value - for example only return
            groups having an average age less than 50.
        order
            Lets you order the returned list by any property that is also present in `by`.
            Only **one** field is allowed at a time.

        Returns
        -------
        List[prisma.types.LessonMaterialGroupByOutput]
            A list of dictionaries representing the LessonMaterial record,
            this will also have additional fields present if aggregation arguments
            are used (see the above parameters)

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # group LessonMaterial records by id values
        # and count how many records are in each group
        results = await LessonMaterial.prisma().group_by(
            ['id'],
            count=True,
        )
        ```
        """
        if order is None:
            if take is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'take\' is present')

            if skip is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'skip\' is present')

        root_selection: List[str] = [*by]
        if avg is not None:
            root_selection.append(_select_fields('_avg', avg))

        if min is not None:
            root_selection.append(_select_fields('_min', min))

        if sum is not None:
            root_selection.append(_select_fields('_sum', sum))

        if max is not None:
            root_selection.append(_select_fields('_max', max))

        if count is not None:
            if count is True:
                root_selection.append('_count { _all }')
            elif isinstance(count, dict):
                root_selection.append(_select_fields('_count', count))

        resp = await self._client._execute(
            method='group_by',
            model=self._model,
            arguments={
                'by': by,
                'take': take,
                'skip': skip,
                'where': where,
                'having': having,
                'orderBy': order,
            },
            root_selection=root_selection,
        )
        return resp['data']['result']  # type: ignore[no-any-return]


class LessonMaterialFileActions(Generic[_PrismaModelT]):
    __slots__ = (
        '_client',
        '_model',
    )

    def __init__(self, client: Prisma, model: Type[_PrismaModelT]) -> None:
        self._client = client
        self._model = model

    async def query_raw(
        self,
        query: LiteralString,
        *args: Any,
    ) -> List[_PrismaModelT]:
        """Execute a raw SQL query

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        List[prisma.models.LessonMaterialFile]
            The records returned by the SQL query

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        users = await LessonMaterialFile.prisma().query_raw(
            'SELECT * FROM LessonMaterialFile WHERE id = $1',
            'bdiicjafbj',
        )
        ```
        """
        return await self._client.query_raw(query, *args, model=self._model)

    async def query_first(
        self,
        query: LiteralString,
        *args: Any,
    ) -> Optional[_PrismaModelT]:
        """Execute a raw SQL query, returning the first result

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        prisma.models.LessonMaterialFile
            The first record returned by the SQL query
        None
            The raw SQL query did not return any records

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        user = await LessonMaterialFile.prisma().query_first(
            'SELECT * FROM LessonMaterialFile WHERE lesson_material_id = $1',
            'bgehebiafc',
        )
        ```
        """
        return await self._client.query_first(query, *args, model=self._model)

    async def create(
        self,
        data: types.LessonMaterialFileCreateInput,
        include: Optional[types.LessonMaterialFileInclude] = None
    ) -> _PrismaModelT:
        """Create a new LessonMaterialFile record.

        Parameters
        ----------
        data
            LessonMaterialFile record data
        include
            Specifies which relations should be loaded on the returned LessonMaterialFile model

        Returns
        -------
        prisma.models.LessonMaterialFile
            The created LessonMaterialFile record

        Raises
        ------
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # create a LessonMaterialFile record from just the required fields
        lessonmaterialfile = await LessonMaterialFile.prisma().create(
            data={
                # data to create a LessonMaterialFile record
                'lesson_material_id': 'bghffegacj',
                'file_type': 'bhghchehcc',
                'original_filename': 'dcgchcbbf',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='create',
            model=self._model,
            arguments={
                'data': data,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def create_many(
        self,
        data: List[types.LessonMaterialFileCreateWithoutRelationsInput],
        *,
        skip_duplicates: Optional[bool] = None,
    ) -> int:
        """Create multiple LessonMaterialFile records at once.

        This function is *not* available when using SQLite.

        Parameters
        ----------
        data
            List of LessonMaterialFile record data
        skip_duplicates
            Boolean flag for ignoring unique constraint errors

        Returns
        -------
        int
            The total number of records created

        Raises
        ------
        prisma.errors.UnsupportedDatabaseError
            Attempting to query when using SQLite
        prisma.errors.UniqueViolationError
            A unique constraint check has failed, these can be ignored with the `skip_duplicates` argument
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        total = await LessonMaterialFile.prisma().create_many(
            data=[
                {
                    # data to create a LessonMaterialFile record
                    'lesson_material_id': 'bdedcabahc',
                    'file_type': 'ghfhiafcb',
                    'original_filename': 'heejgedji',
                },
                {
                    # data to create a LessonMaterialFile record
                    'lesson_material_id': 'bjgjgibgbf',
                    'file_type': 'bbbgbhfjge',
                    'original_filename': 'igbehcbab',
                },
            ],
            skip_duplicates=True,
        )
        ```
        """
        if skip_duplicates and self._client._active_provider in CREATE_MANY_SKIP_DUPLICATES_UNSUPPORTED:
            raise errors.UnsupportedDatabaseError(self._client._active_provider, 'create_many_skip_duplicates')

        resp = await self._client._execute(
            method='create_many',
            model=self._model,
            arguments={
                'data': data,
                'skipDuplicates': skip_duplicates,
            },
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    async def delete(
        self,
        where: types.LessonMaterialFileWhereUniqueInput,
        include: Optional[types.LessonMaterialFileInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Delete a single LessonMaterialFile record.

        Parameters
        ----------
        where
            LessonMaterialFile filter to select the record to be deleted, must be unique
        include
            Specifies which relations should be loaded on the returned LessonMaterialFile model

        Returns
        -------
        prisma.models.LessonMaterialFile
            The deleted LessonMaterialFile record
        None
            Could not find a record to delete

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        lessonmaterialfile = await LessonMaterialFile.prisma().delete(
            where={
                'id': 'bdadaadhag',
            },
        )
        ```
        """
        try:
            resp = await self._client._execute(
                method='delete',
                model=self._model,
                arguments={
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    async def find_unique(
        self,
        where: types.LessonMaterialFileWhereUniqueInput,
        include: Optional[types.LessonMaterialFileInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Find a unique LessonMaterialFile record.

        Parameters
        ----------
        where
            LessonMaterialFile filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned LessonMaterialFile model

        Returns
        -------
        prisma.models.LessonMaterialFile
            The found LessonMaterialFile record
        None
            No record matching the given input could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        lessonmaterialfile = await LessonMaterialFile.prisma().find_unique(
            where={
                'id': 'bgiggdidbf',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_unique',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None
        return model_parse(self._model, result)

    async def find_unique_or_raise(
        self,
        where: types.LessonMaterialFileWhereUniqueInput,
        include: Optional[types.LessonMaterialFileInclude] = None
    ) -> _PrismaModelT:
        """Find a unique LessonMaterialFile record. Raises `RecordNotFoundError` if no record is found.

        Parameters
        ----------
        where
            LessonMaterialFile filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned LessonMaterialFile model

        Returns
        -------
        prisma.models.LessonMaterialFile
            The found LessonMaterialFile record

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        lessonmaterialfile = await LessonMaterialFile.prisma().find_unique_or_raise(
            where={
                'id': 'caaaedabfc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_unique_or_raise',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def find_many(
        self,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.LessonMaterialFileWhereInput] = None,
        cursor: Optional[types.LessonMaterialFileWhereUniqueInput] = None,
        include: Optional[types.LessonMaterialFileInclude] = None,
        order: Optional[Union[types.LessonMaterialFileOrderByInput, List[types.LessonMaterialFileOrderByInput]]] = None,
        distinct: Optional[List[types.LessonMaterialFileScalarFieldKeys]] = None,
    ) -> List[_PrismaModelT]:
        """Find multiple LessonMaterialFile records.

        An empty list is returned if no records could be found.

        Parameters
        ----------
        take
            Limit the maximum number of LessonMaterialFile records returned
        skip
            Ignore the first N results
        where
            LessonMaterialFile filter to select records
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned LessonMaterialFile model
        order
            Order the returned LessonMaterialFile records by any field
        distinct
            Filter LessonMaterialFile records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        List[prisma.models.LessonMaterialFile]
            The list of all LessonMaterialFile records that could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the first 10 LessonMaterialFile records
        lessonmaterialfiles = await LessonMaterialFile.prisma().find_many(take=10)

        # find the first 5 LessonMaterialFile records ordered by the file_type field
        lessonmaterialfiles = await LessonMaterialFile.prisma().find_many(
            take=5,
            order={
                'file_type': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_many',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return [model_parse(self._model, r) for r in resp['data']['result']]

    async def find_first(
        self,
        skip: Optional[int] = None,
        where: Optional[types.LessonMaterialFileWhereInput] = None,
        cursor: Optional[types.LessonMaterialFileWhereUniqueInput] = None,
        include: Optional[types.LessonMaterialFileInclude] = None,
        order: Optional[Union[types.LessonMaterialFileOrderByInput, List[types.LessonMaterialFileOrderByInput]]] = None,
        distinct: Optional[List[types.LessonMaterialFileScalarFieldKeys]] = None,
    ) -> Optional[_PrismaModelT]:
        """Find a single LessonMaterialFile record.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            LessonMaterialFile filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned LessonMaterialFile model
        order
            Order the returned LessonMaterialFile records by any field
        distinct
            Filter LessonMaterialFile records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.LessonMaterialFile
            The first LessonMaterialFile record found, matching the given arguments
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second LessonMaterialFile record ordered by the original_filename field
        lessonmaterialfile = await LessonMaterialFile.prisma().find_first(
            skip=1,
            order={
                'original_filename': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_first',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None

        return model_parse(self._model, result)

    async def find_first_or_raise(
        self,
        skip: Optional[int] = None,
        where: Optional[types.LessonMaterialFileWhereInput] = None,
        cursor: Optional[types.LessonMaterialFileWhereUniqueInput] = None,
        include: Optional[types.LessonMaterialFileInclude] = None,
        order: Optional[Union[types.LessonMaterialFileOrderByInput, List[types.LessonMaterialFileOrderByInput]]] = None,
        distinct: Optional[List[types.LessonMaterialFileScalarFieldKeys]] = None,
    ) -> _PrismaModelT:
        """Find a single LessonMaterialFile record. Raises `RecordNotFoundError` if no record was found.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            LessonMaterialFile filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned LessonMaterialFile model
        order
            Order the returned LessonMaterialFile records by any field
        distinct
            Filter LessonMaterialFile records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.LessonMaterialFile
            The first LessonMaterialFile record found, matching the given arguments

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second LessonMaterialFile record ordered by the object_key field
        lessonmaterialfile = await LessonMaterialFile.prisma().find_first_or_raise(
            skip=1,
            order={
                'object_key': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_first_or_raise',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def update(
        self,
        data: types.LessonMaterialFileUpdateInput,
        where: types.LessonMaterialFileWhereUniqueInput,
        include: Optional[types.LessonMaterialFileInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Update a single LessonMaterialFile record.

        Parameters
        ----------
        data
            LessonMaterialFile record data specifying what to update
        where
            LessonMaterialFile filter to select the unique record to create / update
        include
            Specifies which relations should be loaded on the returned LessonMaterialFile model

        Returns
        -------
        prisma.models.LessonMaterialFile
            The updated LessonMaterialFile record
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        lessonmaterialfile = await LessonMaterialFile.prisma().update(
            where={
                'id': 'bigibebcib',
            },
            data={
                # data to update the LessonMaterialFile record to
            },
        )
        ```
        """
        try:
            resp = await self._client._execute(
                method='update',
                model=self._model,
                arguments={
                    'data': data,
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    async def upsert(
        self,
        where: types.LessonMaterialFileWhereUniqueInput,
        data: types.LessonMaterialFileUpsertInput,
        include: Optional[types.LessonMaterialFileInclude] = None,
    ) -> _PrismaModelT:
        """Updates an existing record or create a new one

        Parameters
        ----------
        where
            LessonMaterialFile filter to select the unique record to create / update
        data
            Data specifying what fields to set on create and update
        include
            Specifies which relations should be loaded on the returned LessonMaterialFile model

        Returns
        -------
        prisma.models.LessonMaterialFile
            The created or updated LessonMaterialFile record

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        lessonmaterialfile = await LessonMaterialFile.prisma().upsert(
            where={
                'id': 'bigaiehgcc',
            },
            data={
                'create': {
                    'id': 'bigaiehgcc',
                    'lesson_material_id': 'bjgjgibgbf',
                    'file_type': 'bbbgbhfjge',
                    'original_filename': 'igbehcbab',
                },
                'update': {
                    'lesson_material_id': 'bjgjgibgbf',
                    'file_type': 'bbbgbhfjge',
                    'original_filename': 'igbehcbab',
                },
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='upsert',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
                'create': data.get('create'),
                'update': data.get('update'),
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def update_many(
        self,
        data: types.LessonMaterialFileUpdateManyMutationInput,
        where: types.LessonMaterialFileWhereInput,
    ) -> int:
        """Update multiple LessonMaterialFile records

        Parameters
        ----------
        data
            LessonMaterialFile data to update the selected LessonMaterialFile records to
        where
            Filter to select the LessonMaterialFile records to update

        Returns
        -------
        int
            The total number of LessonMaterialFile records that were updated

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # update all LessonMaterialFile records
        total = await LessonMaterialFile.prisma().update_many(
            data={
                'minio_bucket': 'beeifcbebf'
            },
            where={}
        )
        ```
        """
        resp = await self._client._execute(
            method='update_many',
            model=self._model,
            arguments={'data': data, 'where': where,},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    @overload
    async def count(
        self,
        select: None = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.LessonMaterialFileWhereInput] = None,
        cursor: Optional[types.LessonMaterialFileWhereUniqueInput] = None,
    ) -> int:
        """Count the number of LessonMaterialFile records present in the database

        Parameters
        ----------
        select
            Select the LessonMaterialFile fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            LessonMaterialFile filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.LessonMaterialFileCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = await LessonMaterialFile.prisma().count()

        # results: prisma.types.LessonMaterialFileCountAggregateOutput
        results = await LessonMaterialFile.prisma().count(
            select={
                '_all': True,
                'file_size': True,
            },
        )
        ```
        """


    @overload
    async def count(
        self,
        select: types.LessonMaterialFileCountAggregateInput,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.LessonMaterialFileWhereInput] = None,
        cursor: Optional[types.LessonMaterialFileWhereUniqueInput] = None,
    ) -> types.LessonMaterialFileCountAggregateOutput:
        ...

    async def count(
        self,
        select: Optional[types.LessonMaterialFileCountAggregateInput] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.LessonMaterialFileWhereInput] = None,
        cursor: Optional[types.LessonMaterialFileWhereUniqueInput] = None,
    ) -> Union[int, types.LessonMaterialFileCountAggregateOutput]:
        """Count the number of LessonMaterialFile records present in the database

        Parameters
        ----------
        select
            Select the LessonMaterialFile fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            LessonMaterialFile filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.LessonMaterialFileCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = await LessonMaterialFile.prisma().count()

        # results: prisma.types.LessonMaterialFileCountAggregateOutput
        results = await LessonMaterialFile.prisma().count(
            select={
                '_all': True,
                'mime_type': True,
            },
        )
        ```
        """

        # TODO: this selection building should be moved to the QueryBuilder
        #
        # note the distinction between checking for `not select` here and `select is None`
        # later is to handle the case that the given select dictionary is empty, this
        # is a limitation of our types.
        if not select:
            root_selection = ['_count { _all }']
        else:

            root_selection = [
                '_count {{ {0} }}'.format(' '.join(k for k, v in select.items() if v is True))
            ]

        resp = await self._client._execute(
            method='count',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'cursor': cursor,
            },
            root_selection=root_selection,
        )

        if select is None:
            return cast(int, resp['data']['result']['_count']['_all'])
        else:
            return cast(types.LessonMaterialFileCountAggregateOutput, resp['data']['result']['_count'])

    async def delete_many(
        self,
        where: Optional[types.LessonMaterialFileWhereInput] = None
    ) -> int:
        """Delete multiple LessonMaterialFile records.

        Parameters
        ----------
        where
            Optional LessonMaterialFile filter to find the records to be deleted

        Returns
        -------
        int
            The total number of LessonMaterialFile records that were deleted

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # delete all LessonMaterialFile records
        total = await LessonMaterialFile.prisma().delete_many()
        ```
        """
        resp = await self._client._execute(
            method='delete_many',
            model=self._model,
            arguments={'where': where},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    # TODO: make this easier to work with safely, currently output fields are typed as
    #       not required, we should refactor the return type
    # TODO: consider returning a Dict where the keys are a Tuple of the `by` selection
    # TODO: statically type that the order argument is required when take or skip are present
    async def group_by(
        self,
        by: List['types.LessonMaterialFileScalarFieldKeys'],
        *,
        where: Optional['types.LessonMaterialFileWhereInput'] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        avg: Optional['types.LessonMaterialFileAvgAggregateInput'] = None,
        sum: Optional['types.LessonMaterialFileSumAggregateInput'] = None,
        min: Optional['types.LessonMaterialFileMinAggregateInput'] = None,
        max: Optional['types.LessonMaterialFileMaxAggregateInput'] = None,
        having: Optional['types.LessonMaterialFileScalarWhereWithAggregatesInput'] = None,
        count: Optional[Union[bool, 'types.LessonMaterialFileCountAggregateInput']] = None,
        order: Optional[Union[Mapping['types.LessonMaterialFileScalarFieldKeys', 'types.SortOrder'], List[Mapping['types.LessonMaterialFileScalarFieldKeys', 'types.SortOrder']]]] = None,
    ) -> List['types.LessonMaterialFileGroupByOutput']:
        """Group LessonMaterialFile records by one or more field values and perform aggregations
        each group such as finding the average.

        Parameters
        ----------
        by
            List of scalar LessonMaterialFile fields to group records by
        where
            LessonMaterialFile filter to select records
        take
            Limit the maximum number of LessonMaterialFile records returned
        skip
            Ignore the first N records
        avg
            Adds the average of all values of the specified fields to the `_avg` field
            in the returned data.
        sum
            Adds the sum of all values of the specified fields to the `_sum` field
            in the returned data.
        min
            Adds the smallest available value for the specified fields to the `_min` field
            in the returned data.
        max
            Adds the largest available value for the specified fields to the `_max` field
            in the returned data.
        count
            Adds a count of non-fields to the `_count` field in the returned data.
        having
            Allows you to filter groups by an aggregate value - for example only return
            groups having an average age less than 50.
        order
            Lets you order the returned list by any property that is also present in `by`.
            Only **one** field is allowed at a time.

        Returns
        -------
        List[prisma.types.LessonMaterialFileGroupByOutput]
            A list of dictionaries representing the LessonMaterialFile record,
            this will also have additional fields present if aggregation arguments
            are used (see the above parameters)

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # group LessonMaterialFile records by page_count values
        # and count how many records are in each group
        results = await LessonMaterialFile.prisma().group_by(
            ['page_count'],
            count=True,
        )
        ```
        """
        if order is None:
            if take is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'take\' is present')

            if skip is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'skip\' is present')

        root_selection: List[str] = [*by]
        if avg is not None:
            root_selection.append(_select_fields('_avg', avg))

        if min is not None:
            root_selection.append(_select_fields('_min', min))

        if sum is not None:
            root_selection.append(_select_fields('_sum', sum))

        if max is not None:
            root_selection.append(_select_fields('_max', max))

        if count is not None:
            if count is True:
                root_selection.append('_count { _all }')
            elif isinstance(count, dict):
                root_selection.append(_select_fields('_count', count))

        resp = await self._client._execute(
            method='group_by',
            model=self._model,
            arguments={
                'by': by,
                'take': take,
                'skip': skip,
                'where': where,
                'having': having,
                'orderBy': order,
            },
            root_selection=root_selection,
        )
        return resp['data']['result']  # type: ignore[no-any-return]


class LessonMaterialLinkActions(Generic[_PrismaModelT]):
    __slots__ = (
        '_client',
        '_model',
    )

    def __init__(self, client: Prisma, model: Type[_PrismaModelT]) -> None:
        self._client = client
        self._model = model

    async def query_raw(
        self,
        query: LiteralString,
        *args: Any,
    ) -> List[_PrismaModelT]:
        """Execute a raw SQL query

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        List[prisma.models.LessonMaterialLink]
            The records returned by the SQL query

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        users = await LessonMaterialLink.prisma().query_raw(
            'SELECT * FROM LessonMaterialLink WHERE id = $1',
            'bgcigfahea',
        )
        ```
        """
        return await self._client.query_raw(query, *args, model=self._model)

    async def query_first(
        self,
        query: LiteralString,
        *args: Any,
    ) -> Optional[_PrismaModelT]:
        """Execute a raw SQL query, returning the first result

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        prisma.models.LessonMaterialLink
            The first record returned by the SQL query
        None
            The raw SQL query did not return any records

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        user = await LessonMaterialLink.prisma().query_first(
            'SELECT * FROM LessonMaterialLink WHERE lesson_material_id = $1',
            'bcejgaggif',
        )
        ```
        """
        return await self._client.query_first(query, *args, model=self._model)

    async def create(
        self,
        data: types.LessonMaterialLinkCreateInput,
        include: Optional[types.LessonMaterialLinkInclude] = None
    ) -> _PrismaModelT:
        """Create a new LessonMaterialLink record.

        Parameters
        ----------
        data
            LessonMaterialLink record data
        include
            Specifies which relations should be loaded on the returned LessonMaterialLink model

        Returns
        -------
        prisma.models.LessonMaterialLink
            The created LessonMaterialLink record

        Raises
        ------
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # create a LessonMaterialLink record from just the required fields
        lessonmateriallink = await LessonMaterialLink.prisma().create(
            data={
                # data to create a LessonMaterialLink record
                'lesson_material_id': 'idfjadbcc',
                'from_exercise_id': 'hgdhbjhhj',
                'to_exercise_id': 'ecjjjfbae',
                'relationship_type': 'bhhfibbigf',
                'confidence': 893145566.99540,
                'rationale': 'cbachdgfce',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='create',
            model=self._model,
            arguments={
                'data': data,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def create_many(
        self,
        data: List[types.LessonMaterialLinkCreateWithoutRelationsInput],
        *,
        skip_duplicates: Optional[bool] = None,
    ) -> int:
        """Create multiple LessonMaterialLink records at once.

        This function is *not* available when using SQLite.

        Parameters
        ----------
        data
            List of LessonMaterialLink record data
        skip_duplicates
            Boolean flag for ignoring unique constraint errors

        Returns
        -------
        int
            The total number of records created

        Raises
        ------
        prisma.errors.UnsupportedDatabaseError
            Attempting to query when using SQLite
        prisma.errors.UniqueViolationError
            A unique constraint check has failed, these can be ignored with the `skip_duplicates` argument
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        total = await LessonMaterialLink.prisma().create_many(
            data=[
                {
                    # data to create a LessonMaterialLink record
                    'lesson_material_id': 'chbfcacbd',
                    'from_exercise_id': 'efggddide',
                    'to_exercise_id': 'caficfigfb',
                    'relationship_type': 'bfidgijfjc',
                    'confidence': 878442065.167528,
                    'rationale': 'bgchfhgceh',
                },
                {
                    # data to create a LessonMaterialLink record
                    'lesson_material_id': 'cafeiaccbc',
                    'from_exercise_id': 'gaddfhfh',
                    'to_exercise_id': 'gieegcbeg',
                    'relationship_type': 'bgcffadich',
                    'confidence': 521827728.126603,
                    'rationale': 'jdcfdcgc',
                },
            ],
            skip_duplicates=True,
        )
        ```
        """
        if skip_duplicates and self._client._active_provider in CREATE_MANY_SKIP_DUPLICATES_UNSUPPORTED:
            raise errors.UnsupportedDatabaseError(self._client._active_provider, 'create_many_skip_duplicates')

        resp = await self._client._execute(
            method='create_many',
            model=self._model,
            arguments={
                'data': data,
                'skipDuplicates': skip_duplicates,
            },
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    async def delete(
        self,
        where: types.LessonMaterialLinkWhereUniqueInput,
        include: Optional[types.LessonMaterialLinkInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Delete a single LessonMaterialLink record.

        Parameters
        ----------
        where
            LessonMaterialLink filter to select the record to be deleted, must be unique
        include
            Specifies which relations should be loaded on the returned LessonMaterialLink model

        Returns
        -------
        prisma.models.LessonMaterialLink
            The deleted LessonMaterialLink record
        None
            Could not find a record to delete

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        lessonmateriallink = await LessonMaterialLink.prisma().delete(
            where={
                'id': 'cafdaehjid',
            },
        )
        ```
        """
        try:
            resp = await self._client._execute(
                method='delete',
                model=self._model,
                arguments={
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    async def find_unique(
        self,
        where: types.LessonMaterialLinkWhereUniqueInput,
        include: Optional[types.LessonMaterialLinkInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Find a unique LessonMaterialLink record.

        Parameters
        ----------
        where
            LessonMaterialLink filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned LessonMaterialLink model

        Returns
        -------
        prisma.models.LessonMaterialLink
            The found LessonMaterialLink record
        None
            No record matching the given input could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        lessonmateriallink = await LessonMaterialLink.prisma().find_unique(
            where={
                'id': 'gifdddbia',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_unique',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None
        return model_parse(self._model, result)

    async def find_unique_or_raise(
        self,
        where: types.LessonMaterialLinkWhereUniqueInput,
        include: Optional[types.LessonMaterialLinkInclude] = None
    ) -> _PrismaModelT:
        """Find a unique LessonMaterialLink record. Raises `RecordNotFoundError` if no record is found.

        Parameters
        ----------
        where
            LessonMaterialLink filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned LessonMaterialLink model

        Returns
        -------
        prisma.models.LessonMaterialLink
            The found LessonMaterialLink record

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        lessonmateriallink = await LessonMaterialLink.prisma().find_unique_or_raise(
            where={
                'id': 'bchehecef',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_unique_or_raise',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def find_many(
        self,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.LessonMaterialLinkWhereInput] = None,
        cursor: Optional[types.LessonMaterialLinkWhereUniqueInput] = None,
        include: Optional[types.LessonMaterialLinkInclude] = None,
        order: Optional[Union[types.LessonMaterialLinkOrderByInput, List[types.LessonMaterialLinkOrderByInput]]] = None,
        distinct: Optional[List[types.LessonMaterialLinkScalarFieldKeys]] = None,
    ) -> List[_PrismaModelT]:
        """Find multiple LessonMaterialLink records.

        An empty list is returned if no records could be found.

        Parameters
        ----------
        take
            Limit the maximum number of LessonMaterialLink records returned
        skip
            Ignore the first N results
        where
            LessonMaterialLink filter to select records
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned LessonMaterialLink model
        order
            Order the returned LessonMaterialLink records by any field
        distinct
            Filter LessonMaterialLink records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        List[prisma.models.LessonMaterialLink]
            The list of all LessonMaterialLink records that could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the first 10 LessonMaterialLink records
        lessonmateriallinks = await LessonMaterialLink.prisma().find_many(take=10)

        # find the first 5 LessonMaterialLink records ordered by the from_exercise_id field
        lessonmateriallinks = await LessonMaterialLink.prisma().find_many(
            take=5,
            order={
                'from_exercise_id': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_many',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return [model_parse(self._model, r) for r in resp['data']['result']]

    async def find_first(
        self,
        skip: Optional[int] = None,
        where: Optional[types.LessonMaterialLinkWhereInput] = None,
        cursor: Optional[types.LessonMaterialLinkWhereUniqueInput] = None,
        include: Optional[types.LessonMaterialLinkInclude] = None,
        order: Optional[Union[types.LessonMaterialLinkOrderByInput, List[types.LessonMaterialLinkOrderByInput]]] = None,
        distinct: Optional[List[types.LessonMaterialLinkScalarFieldKeys]] = None,
    ) -> Optional[_PrismaModelT]:
        """Find a single LessonMaterialLink record.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            LessonMaterialLink filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned LessonMaterialLink model
        order
            Order the returned LessonMaterialLink records by any field
        distinct
            Filter LessonMaterialLink records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.LessonMaterialLink
            The first LessonMaterialLink record found, matching the given arguments
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second LessonMaterialLink record ordered by the to_exercise_id field
        lessonmateriallink = await LessonMaterialLink.prisma().find_first(
            skip=1,
            order={
                'to_exercise_id': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_first',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None

        return model_parse(self._model, result)

    async def find_first_or_raise(
        self,
        skip: Optional[int] = None,
        where: Optional[types.LessonMaterialLinkWhereInput] = None,
        cursor: Optional[types.LessonMaterialLinkWhereUniqueInput] = None,
        include: Optional[types.LessonMaterialLinkInclude] = None,
        order: Optional[Union[types.LessonMaterialLinkOrderByInput, List[types.LessonMaterialLinkOrderByInput]]] = None,
        distinct: Optional[List[types.LessonMaterialLinkScalarFieldKeys]] = None,
    ) -> _PrismaModelT:
        """Find a single LessonMaterialLink record. Raises `RecordNotFoundError` if no record was found.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            LessonMaterialLink filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned LessonMaterialLink model
        order
            Order the returned LessonMaterialLink records by any field
        distinct
            Filter LessonMaterialLink records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.LessonMaterialLink
            The first LessonMaterialLink record found, matching the given arguments

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second LessonMaterialLink record ordered by the relationship_type field
        lessonmateriallink = await LessonMaterialLink.prisma().find_first_or_raise(
            skip=1,
            order={
                'relationship_type': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_first_or_raise',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def update(
        self,
        data: types.LessonMaterialLinkUpdateInput,
        where: types.LessonMaterialLinkWhereUniqueInput,
        include: Optional[types.LessonMaterialLinkInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Update a single LessonMaterialLink record.

        Parameters
        ----------
        data
            LessonMaterialLink record data specifying what to update
        where
            LessonMaterialLink filter to select the unique record to create / update
        include
            Specifies which relations should be loaded on the returned LessonMaterialLink model

        Returns
        -------
        prisma.models.LessonMaterialLink
            The updated LessonMaterialLink record
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        lessonmateriallink = await LessonMaterialLink.prisma().update(
            where={
                'id': 'jeijcbhfe',
            },
            data={
                # data to update the LessonMaterialLink record to
            },
        )
        ```
        """
        try:
            resp = await self._client._execute(
                method='update',
                model=self._model,
                arguments={
                    'data': data,
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    async def upsert(
        self,
        where: types.LessonMaterialLinkWhereUniqueInput,
        data: types.LessonMaterialLinkUpsertInput,
        include: Optional[types.LessonMaterialLinkInclude] = None,
    ) -> _PrismaModelT:
        """Updates an existing record or create a new one

        Parameters
        ----------
        where
            LessonMaterialLink filter to select the unique record to create / update
        data
            Data specifying what fields to set on create and update
        include
            Specifies which relations should be loaded on the returned LessonMaterialLink model

        Returns
        -------
        prisma.models.LessonMaterialLink
            The created or updated LessonMaterialLink record

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        lessonmateriallink = await LessonMaterialLink.prisma().upsert(
            where={
                'id': 'bjgejjabff',
            },
            data={
                'create': {
                    'id': 'bjgejjabff',
                    'lesson_material_id': 'cafeiaccbc',
                    'from_exercise_id': 'gaddfhfh',
                    'to_exercise_id': 'gieegcbeg',
                    'relationship_type': 'bgcffadich',
                    'confidence': 521827728.126603,
                    'rationale': 'jdcfdcgc',
                },
                'update': {
                    'lesson_material_id': 'cafeiaccbc',
                    'from_exercise_id': 'gaddfhfh',
                    'to_exercise_id': 'gieegcbeg',
                    'relationship_type': 'bgcffadich',
                    'confidence': 521827728.126603,
                    'rationale': 'jdcfdcgc',
                },
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='upsert',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
                'create': data.get('create'),
                'update': data.get('update'),
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def update_many(
        self,
        data: types.LessonMaterialLinkUpdateManyMutationInput,
        where: types.LessonMaterialLinkWhereInput,
    ) -> int:
        """Update multiple LessonMaterialLink records

        Parameters
        ----------
        data
            LessonMaterialLink data to update the selected LessonMaterialLink records to
        where
            Filter to select the LessonMaterialLink records to update

        Returns
        -------
        int
            The total number of LessonMaterialLink records that were updated

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # update all LessonMaterialLink records
        total = await LessonMaterialLink.prisma().update_many(
            data={
                'confidence': 1228891816.25520
            },
            where={}
        )
        ```
        """
        resp = await self._client._execute(
            method='update_many',
            model=self._model,
            arguments={'data': data, 'where': where,},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    @overload
    async def count(
        self,
        select: None = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.LessonMaterialLinkWhereInput] = None,
        cursor: Optional[types.LessonMaterialLinkWhereUniqueInput] = None,
    ) -> int:
        """Count the number of LessonMaterialLink records present in the database

        Parameters
        ----------
        select
            Select the LessonMaterialLink fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            LessonMaterialLink filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.LessonMaterialLinkCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = await LessonMaterialLink.prisma().count()

        # results: prisma.types.LessonMaterialLinkCountAggregateOutput
        results = await LessonMaterialLink.prisma().count(
            select={
                '_all': True,
                'rationale': True,
            },
        )
        ```
        """


    @overload
    async def count(
        self,
        select: types.LessonMaterialLinkCountAggregateInput,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.LessonMaterialLinkWhereInput] = None,
        cursor: Optional[types.LessonMaterialLinkWhereUniqueInput] = None,
    ) -> types.LessonMaterialLinkCountAggregateOutput:
        ...

    async def count(
        self,
        select: Optional[types.LessonMaterialLinkCountAggregateInput] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.LessonMaterialLinkWhereInput] = None,
        cursor: Optional[types.LessonMaterialLinkWhereUniqueInput] = None,
    ) -> Union[int, types.LessonMaterialLinkCountAggregateOutput]:
        """Count the number of LessonMaterialLink records present in the database

        Parameters
        ----------
        select
            Select the LessonMaterialLink fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            LessonMaterialLink filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.LessonMaterialLinkCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = await LessonMaterialLink.prisma().count()

        # results: prisma.types.LessonMaterialLinkCountAggregateOutput
        results = await LessonMaterialLink.prisma().count(
            select={
                '_all': True,
                'created_at': True,
            },
        )
        ```
        """

        # TODO: this selection building should be moved to the QueryBuilder
        #
        # note the distinction between checking for `not select` here and `select is None`
        # later is to handle the case that the given select dictionary is empty, this
        # is a limitation of our types.
        if not select:
            root_selection = ['_count { _all }']
        else:

            root_selection = [
                '_count {{ {0} }}'.format(' '.join(k for k, v in select.items() if v is True))
            ]

        resp = await self._client._execute(
            method='count',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'cursor': cursor,
            },
            root_selection=root_selection,
        )

        if select is None:
            return cast(int, resp['data']['result']['_count']['_all'])
        else:
            return cast(types.LessonMaterialLinkCountAggregateOutput, resp['data']['result']['_count'])

    async def delete_many(
        self,
        where: Optional[types.LessonMaterialLinkWhereInput] = None
    ) -> int:
        """Delete multiple LessonMaterialLink records.

        Parameters
        ----------
        where
            Optional LessonMaterialLink filter to find the records to be deleted

        Returns
        -------
        int
            The total number of LessonMaterialLink records that were deleted

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # delete all LessonMaterialLink records
        total = await LessonMaterialLink.prisma().delete_many()
        ```
        """
        resp = await self._client._execute(
            method='delete_many',
            model=self._model,
            arguments={'where': where},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    # TODO: make this easier to work with safely, currently output fields are typed as
    #       not required, we should refactor the return type
    # TODO: consider returning a Dict where the keys are a Tuple of the `by` selection
    # TODO: statically type that the order argument is required when take or skip are present
    async def group_by(
        self,
        by: List['types.LessonMaterialLinkScalarFieldKeys'],
        *,
        where: Optional['types.LessonMaterialLinkWhereInput'] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        avg: Optional['types.LessonMaterialLinkAvgAggregateInput'] = None,
        sum: Optional['types.LessonMaterialLinkSumAggregateInput'] = None,
        min: Optional['types.LessonMaterialLinkMinAggregateInput'] = None,
        max: Optional['types.LessonMaterialLinkMaxAggregateInput'] = None,
        having: Optional['types.LessonMaterialLinkScalarWhereWithAggregatesInput'] = None,
        count: Optional[Union[bool, 'types.LessonMaterialLinkCountAggregateInput']] = None,
        order: Optional[Union[Mapping['types.LessonMaterialLinkScalarFieldKeys', 'types.SortOrder'], List[Mapping['types.LessonMaterialLinkScalarFieldKeys', 'types.SortOrder']]]] = None,
    ) -> List['types.LessonMaterialLinkGroupByOutput']:
        """Group LessonMaterialLink records by one or more field values and perform aggregations
        each group such as finding the average.

        Parameters
        ----------
        by
            List of scalar LessonMaterialLink fields to group records by
        where
            LessonMaterialLink filter to select records
        take
            Limit the maximum number of LessonMaterialLink records returned
        skip
            Ignore the first N records
        avg
            Adds the average of all values of the specified fields to the `_avg` field
            in the returned data.
        sum
            Adds the sum of all values of the specified fields to the `_sum` field
            in the returned data.
        min
            Adds the smallest available value for the specified fields to the `_min` field
            in the returned data.
        max
            Adds the largest available value for the specified fields to the `_max` field
            in the returned data.
        count
            Adds a count of non-fields to the `_count` field in the returned data.
        having
            Allows you to filter groups by an aggregate value - for example only return
            groups having an average age less than 50.
        order
            Lets you order the returned list by any property that is also present in `by`.
            Only **one** field is allowed at a time.

        Returns
        -------
        List[prisma.types.LessonMaterialLinkGroupByOutput]
            A list of dictionaries representing the LessonMaterialLink record,
            this will also have additional fields present if aggregation arguments
            are used (see the above parameters)

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # group LessonMaterialLink records by id values
        # and count how many records are in each group
        results = await LessonMaterialLink.prisma().group_by(
            ['id'],
            count=True,
        )
        ```
        """
        if order is None:
            if take is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'take\' is present')

            if skip is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'skip\' is present')

        root_selection: List[str] = [*by]
        if avg is not None:
            root_selection.append(_select_fields('_avg', avg))

        if min is not None:
            root_selection.append(_select_fields('_min', min))

        if sum is not None:
            root_selection.append(_select_fields('_sum', sum))

        if max is not None:
            root_selection.append(_select_fields('_max', max))

        if count is not None:
            if count is True:
                root_selection.append('_count { _all }')
            elif isinstance(count, dict):
                root_selection.append(_select_fields('_count', count))

        resp = await self._client._execute(
            method='group_by',
            model=self._model,
            arguments={
                'by': by,
                'take': take,
                'skip': skip,
                'where': where,
                'having': having,
                'orderBy': order,
            },
            root_selection=root_selection,
        )
        return resp['data']['result']  # type: ignore[no-any-return]



def _select_fields(root: str, select: Mapping[str, Any]) -> str:
    """Helper to build a GraphQL selection string

    This is a work around until field selection is added to the query builder.
    """

    return root + ' {{ {0} }}'.format(' '.join(k for k, v in select.items() if v is True))


from . import models